CiosaiGL is a WebGL2 wrapper for browser demoscene productions

vite single file squeezes it into one singular html file

start by running `deno task build` and view the `dist/index.html` with your browser
